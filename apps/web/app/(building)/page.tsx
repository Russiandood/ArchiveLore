export const dynamic = 'force-dynamic';

export default function BuildingRootPlaceholder() {
  return (
    <div style={{ padding: 24 }}>
      <h1>(building) root placeholder</h1>
      <p>If you see this at /, the (building) layout is active.</p>
    </div>
  );
}
